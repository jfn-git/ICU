# 20 — Session handoff (2026-05-24)

> 給下一個 session / 組員的接手紀錄。submission deadline **2026-05-25 23:59**(剩約 1.5 天)。
> 繁中說明,paper 內容英文。最權威的狀態以 `git log` 與檔案本身為準。

---

## 0. TL;DR 現在在哪裡

合併後的投稿草稿 **`paper/paper_v1.tex`** 已是「submission-clean」:
- A-led 故事(診斷 mean-imputation 的 OOD-action failure + 機制定位 + support-aware remedy 光譜),hero = component ablation + Q-leakage。
- 隊友的跨演算法 + AA-MBRL breadth **已用我們的 exact V^π 重評併入**(同軸、全部 ≤ V\*=0.875、無 rollout 超界)。
- 無 `\todo`、無 provisional/★ 標記、references 乾淨(無 "Verified via" 註)。
- 主文壓在 8 頁(經兩次微調);exact 學習曲線在主文 §6.3,penalty 圖在 appendix。

**尚未確認的唯一硬點**:主文是否真的 ≤8 頁 — 要在 Overleaf 實際編譯確認(我方無法本地編譯,缺 `neurips_2026.sty`;隊友 repo 有)。

---

## 1. Git 狀態(重要)

- 分支 `main`。最新 7 個 commit(新到舊):
  - `e6923d5` Promote exact learning-curve to main (§6.3); move penalty to appendix
  - `6513a89` Add exact dist-V* learning-curve figure
  - `8edf1f2` Trim ~0.4 page (keep main ≤8 pages)
  - `e539c49` Address Codex round-2 (clean bib, scope tabular-Q, soften overclaims)
  - `eff846d` Remove appendix TODO placeholders
  - `a08054d` Fill Related Work citations (sb3 / Wu / Nair)
  - `70a2aeb` Integrate teammate breadth via exact V-π re-eval; add merged paper_v1
- **本地領先 origin/main 3 個 commit(未推):`8edf1f2`、`6513a89`、`e6923d5`。** 直推 main 被 harness 擋,**由人類手動 `git push origin main`**。
- 工作樹乾淨,只有一個 untracked build-artifact `Formatting_Instructions_For_NeurIPS_2026__1_.pdf`(已從 index 取消;**不要 git add**;可考慮 gitignore)。

---

## 2. Paper 關鍵事實 / 數字(全 exact V^π,J\*=0.8751)

- 參考:Random 0.780 / Clinician 0.782 / Optimal 0.875。
- vanilla Q:dist-V\* **0.090**、deploy-unsupp **~87%**、Q-leakage **+0.46**,J≈baseline → 失敗被 survival rate 藏住。
- 機制(component ablation,repo ✅):behavior-only == full mask = **0.069**;target-only 0.085 / policy-only 0.083(deploy 0% 但 leakage 仍 +0.46);CQL κ=1 0.080(best soft);leakage +0.46→−0.80。
- penalty sweep:plateau ~0.085,被 hard mask 0.069 Pareto-dominated。
- offline(T2,repo ✅):naive 高估 +0.21 且停滯 ~0.093;pessimism 校準→0.069;masking→0.065。
- **隊友 breadth(exact 重評,Table 2)**:baselines Sarsa .789/Q .787/DQN .799/PPO .842;+mask AA-Sarsa .802/AA-Q .801/AA-DQN .826/AA-PPO .869;**AA-MBRL .874(gap .001)**。mask gains +0.013/.014/.027/.027。
  - ⚠️ 她論文的「AA-MBRL=0.874」對應 code 的 **greedy** tag(exact 0.874),不是 UCB tag(exact 只有 0.803)。MBRL 的 SOFA/KL 變體在 exact 下不分高下(全 ~0.8737)→ 她「KL 唯一達 oracle」是 rollout 假象。

---

## 3. 已鎖定的決策

1. **一隊一篇,A-led**:診斷/機制為主軸;隊友的廣度/AA-MBRL 一律 supporting evidence、非 headline。
2. **指標**:全篇 exact V^π / dist-V\*(不是 benchmark 的 rollout return)。這是賣點;隊友用 rollout(會超過 V\*),我們重評她的 policy 解決。
3. **AA-MBRL** = remedy 光譜 model-based 端點,明說「not a fair model-free comparison」、非 SOTA。
4. **不 overclaim**:無 "first"/"structure beats capacity"/"ships the cure";Conclusion 用「benchmark exposes the support info to diagnose and mitigate」。
5. **Training curve** 用 exact dist-V\* 版(放主文 §6.3),隊友的 rollout tier 曲線若要放只進 appendix 並標清楚指標。

---

## 4. 可重現性 / 關鍵檔案

- `paper/paper_v1.tex` — 投稿草稿(主檔)。`paper/references.bib` — 已清乾淨,含 sb3/Wu/Nair/UCRL/PSRL。
- `paper/main.tex` — 舊骨架(保留,勿用於投稿)。
- 我方實驗:`submission/scripts/run_*.py` + `results/*/summary.json`(baselines/penalty_sweep/component_ablation/offline_datasize/expert_prior/robustness/deadend)。圖在 `submission/figures/`。
- **隊友 policy 重評**:`results/teammate_policies/`(85 個 .npy,vendored)+ `scripts/eval_teammate_policies.py` → `results/teammate_exact_eval/summary.json`。重跑:`python scripts/eval_teammate_policies.py`。
- **exact 學習曲線**:`scripts/plot_exact_learning_curve.py` → `submission/figures/exact_dist_curve.png`。
- 隊友原始 repo(clone,非 repo 內):`C:/Users/jeff9/aa-rl-teammate`(github `jaylin0418/aa-rl-icusepsis`)。
- 規格文件:`core_docs/17_merged_paper_outline.md`(架構+§9 external validity 草稿)、`core_docs/paper_spec.md`(逐章節施工圖+頂部 🔒 v1 決策)、`core_docs/spec.md`(NeurIPS 寫作規格)、`core_docs/18`(隊友 paper 解讀)、`core_docs/19`(合併團隊簡報)。

---

## 5. 投稿前未了事項(deadline 5/25)

| 優先 | 事項 |
|---|---|
| 🔴 | **`git push origin main`**(3 個未推 commit) |
| 🔴 | **Overleaf 編譯確認**:用隊友 repo 的 `neurips_2026.sty`;重貼 `paper_v1.tex` + 重傳 `references.bib`;上傳新圖 `exact_dist_curve.png`;**Recompile ×2**;確認 → 無紅字、無 `[?]`、參考文獻無 "Verified via"、**主文(到 Conclusion)≤8 頁**、§6.3 有學習曲線、penalty 在 appendix |
| 🟡 | 若主文 >8 頁:依序 hero 圖 0.95→0.85、Table 2 group 分隔合併、或壓 Discussion 一段(見 §17 槓桿) |
| 🟡 | **匿名稽核**:全文無姓名/校名/真實 repo URL(目前只寫 "released repo",OK);camera-ready 才放匿名連結 |
| ⚪ | (加分)overview Fig 1 schematic;隊友 rollout tier 圖放 appendix(若她堅持) |
| ⚪ | bib metadata camera-ready 前再核(auer/osband/raffin 等是常識引用,內容正確,格式可再查) |

---

## 6. 已知 gotcha

- **無法本地編譯**(缺 `.sty`)→ 頁數/紅字只能在 Overleaf 確認;我這邊一切都靠結構估算。
- Bash 工具是 bash 不是 PowerShell;commit message 用多個 `-m`(別用 PowerShell here-string `@'...'@`,會壞)。
- 直推 main 會被 harness 擋 → 人類手動推。
- CRLF 警告無害(Windows)。
- Codex 兩輪 review 都處理完了(round-2 的 6 點全做);若再來新 review,先判斷是否與「A-led / exact 軸 / 不 overclaim」一致再採納。
