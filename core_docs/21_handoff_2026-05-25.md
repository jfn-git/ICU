# Session handoff — 2026-05-25 (paper polish, section-by-section review)

> Continues from `20_handoff_2026-05-24.md`. This session focused on (a) submission
> infrastructure (checklist, anonymized code zip, mean-imputation verification),
> (b) fixing a real number error in Table 3 (AA-MBRL), and (c) a section-by-section
> writing review of `paper_final/main.tex` with Codex (Abstract → §5.2 done).

## TL;DR for the next session
- Paper lives in **`paper_final/`** (`main.tex`, `checklist.tex`, `neurips_2026.sty`,
  `references.bib`, `figures/`). It is self-contained and compiles on Overleaf.
- Work branch: **`paper-final-submission`**, latest pushed commit **`f9bfd52`**.
  `main` is NOT updated (merge via PR or authorized direct push when ready).
- **Deadline: 2026-05-25 23:59** (today). Submit on OpenReview: PDF **+**
  `supplementary_code.zip` (repo root, git-ignored).
- Review is in progress: **Abstract, Introduction, §5 Setup/Baselines, §5.1, §5.2 done.**
  Next: §2 Related Work, §3 Background, §4 Method, §5.3, §5.4, §5.5, §6–§8, Reproducibility, Appendix.

## Authoritative data sources (use these for every number; do NOT hand-edit numbers)
- `results/teammate_exact_eval/summary.json` — **5×10⁵ cross-algorithm, exact V^π** (Table 3).
  `j_star = 0.87514`. Key: QLearning 0.78684, Sarsa 0.78847, DQN 0.79914, PPO 0.84205;
  AA_Sarsa 0.80218, AA_QLearning 0.80133, AA_DQN_T 0.82558, AA_PPO_T 0.86902;
  **AA_MBRL_greedy 0.87367** (this is the paper's "AA-MBRL"), AA_MBRL_UCB 0.80287 (deprecated).
- `results/component_ablation/summary.json` — **5×10⁴ mechanism** (Table 2 / Fig 1):
  vanilla dist 0.08960 / deploy 0.87069 / leak +0.4594; behavior_only & full_mask dist 0.06934 / leak −0.7951;
  target_only dist 0.0850 / deploy 0.769 / leak +0.070; policy_only dist 0.0834 / deploy 0 / leak +0.4594;
  conservative_k1 (CQL κ=1) dist 0.0801 / leak −2.951; conservative_k2 dist 0.0894.
- `results/baselines/summary.json` — Random 0.78007, Expert 0.78184, Optimal 0.87514.
- `results/offline_datasize/summary.json`, `results/deadend/summary.json` — for §5.4 / §5.5.

## Key decisions made this session (carry these forward)
1. **AA-MBRL = greedy** (teammate confirmed). Exact J = **0.874 / gap 0.001** (P1+P2). The
   **UCB** variant (exact 0.803) is deprecated and must NOT appear in the paper. Earlier I
   briefly had a wrong "UCB 0.803 vs 0.874" contrast — it is removed; do not reintroduce it.
2. **CQL removed from the component ablation** (§5.2 Table 2 + Figure 1). The component
   ablation is now the 5 masking locations only (vanilla / target-only / policy-only /
   behavior-only / full mask). CQL lives only in **§5.3 remedy spectrum** (+ App penalty
   figure + exact-curve figure). §5.2 keeps one forward-ref "(CQL; §5.3)".
3. **J\* vs V\* convention**: scalar optimal value (0.875) = **`$J^\star$`** (abstract, intro,
   both table "Optimal" headers). The value *function* / dist target stays **`$V^\star$`**
   (`$V^\star(s)$`, "gap to $V^\star$", `$\min_s V^\star(s)$`, "$\le V^\star$").
4. **`7×10⁻¹⁶` mean-imputation claim is verified and reproducible** — `scripts/verify_mean_imputation.py`
   prints max |inadmissible row − admissible mean| = **7.216e-16** over 15,587 pairs. Keep §3 wording.
5. **vanilla J = 0.785** is a deliberate round-down of the true 0.78555 so that
   `J + dist = J* = 0.875` stays internally consistent. Do not "fix" to 0.786 (it would break the sum).
6. **Figure 1 is regenerated by `scripts/plot_component_ablation.py`** (reads results JSON,
   NO retraining). Re-run it if the figure ever needs changing; do not edit the PNG by hand.
7. **±std added to Table 3** (exact J column), from `teammate_exact_eval/summary.json` J_std.

## Section-by-section review status (with Codex)
| Section | Status |
|---|---|
| Abstract | ✅ kept terse (J / Q-leakage are glossed in the Intro instead) |
| §1 Introduction | ✅ Codex version: J defined as `$d_0$-weighted survival value`; Q-leakage defined inline; `$\dist$`→"exact value gap to $V^\star$"; contributions reworded ("corroborate this conclusion"); kept simple "behavior-level sampling" (no "and bootstrapping") |
| §5 Setup + Baselines | ✅ clearer prose; both "Optimal" headers → `$J^\star$` |
| §5.1 Claim 1 | ✅ two-paragraph clean vanilla-vs-AA contrast |
| §5.2 Claim 2 | ✅ dropped "necessary & sufficient" / "hero" wording; CQL removed; Fig 1 regenerated |
| §2 Related Work | ⬜ not yet reviewed |
| §3 Background | ⬜ (verify τ=20, 716 states, median 4 / mean 3.2/3.1, Eq.(mean), 7e-16 — all already checked correct) |
| §4 Method (Diagnostic Protocol) | ⬜ |
| §5.3 Remedy spectrum (Claim 3a) | ⬜ (contains CQL + Table 3 + exact-curve figure) |
| §5.4 Offline (Claim 3b) | ⬜ |
| §5.5 Supplementary | ⬜ |
| §6 Discussion / §7 Limitations / §8 Conclusion / Reproducibility | ⬜ |
| Appendix figures | ⬜ |

Review workflow: user pastes a Codex-revised passage; analyze it for (1) number consistency
vs the data JSONs + rest of paper, (2) over-claims (avoid "necessary&sufficient", "hero",
"nearly closes"), (3) scope (model-free is the main story; model-based/cross-algo/offline are
supporting), (4) anonymity, (5) internal cross-references. Then apply if approved.

## Outstanding TODO before submission
- [ ] Finish section review (Related Work → Conclusion + Appendix).
- [ ] **Rebuild `supplementary_code.zip`** — it is stale: it must include the two newest
      scripts `scripts/plot_component_ablation.py` and `scripts/verify_mean_imputation.py`.
      (Build recipe: stage `src/ scripts/ configs/ tests/test_sanity.py requirements.txt` +
      an anonymized README into a `code/` folder, scan for identity leaks, `Compress-Archive`.)
- [ ] **Final Overleaf layout pass**: Recompile ×2; confirm ≤8 content pages (References from
      p.9); check Table 3 width now that it has ±std; check Figure 1 / Table 2 float placement
      (currently `[t]/[tb]` — standard; a deferred-float gap can appear, retune only at the end).
- [ ] Upload on OpenReview: PDF **+** `supplementary_code.zip` (Q5 of checklist says code is provided).
- [ ] Anonymity: repo `github.com/jfn-git/ICU` is the author's; paper has no link to it (verified).
      Consider keeping it private during double-blind review (until 2026-06-08).
- [ ] (optional) `scripts/eval_teammate_policies.py` has a stale label
      `HER_ROLLOUT["AA_MBRL_UCB"]=0.874` — teammate's AA-MBRL is the greedy variant; cosmetic only.
- [ ] (optional) stale comment `% --- HERO FIGURE ...` above Fig 1 (caption no longer says "hero").
- [ ] (optional) add a half-sentence to Fig 1 caption noting right-panel CI bands are narrow
      (cross-seed variance is small) — currently invisible because variance is tiny (≈good).

## Git / files
- Branch `paper-final-submission` @ `f9bfd52` (pushed). PR: https://github.com/jfn-git/ICU/pull/new/paper-final-submission
- `.gitignore` now ignores `*.zip`. `supplementary_code.zip` is intentionally untracked.
- Compile: Overleaf, upload `paper_final/` as-is, anonymous mode (do NOT pass `[final]`), Recompile ×2.

## Two unrelated dirs (don't confuse)
- `github.com/jfn-git/ICU` = our repo (this codebase, our paper).
- `github.com/jaylin0418/aa-rl-icusepsis` = teammate's repo (the cross-algorithm pipeline;
  title "Admissibility-Aware RL Closes the Optimality Gap"). Its `data/summary/e1_main_table.md`
  is **rollout** return; our Table 3 is the **exact** re-evaluation of those saved policies.
